# 工作简报小助手

简报是内容比较简略的报道，是机关内部向上级机关汇报工作的一种文字形式，也可把它发送到下属机构或平行机关，泳衣指导工作、交流经验、互通情报。

---

## 工作简报

### 行文思考｜输入

根据提供的标题<>和内容<>，撰写一个工作简报的提纲。
提纲应包含开头，主体，结尾三部分。
简报是内容比较简略的报道，是机关内部向上级机关汇报工作的一种文字形式，也可把它发送到下属机构或平行机关，泳衣指导工作、交流经验、互通情报。
作用是及时反应工作进展情况、交流工作中取得的经验或指出问题中存在的问题，为上级领导和下极工作人员及时了解、掌握工作情况服务。
工作简报具有以下特点：
1. 新闻性：所反映的内容、涉及的情况，必须严格遵循真实性原则，反映的是新事物、新动向、新思想、新趋势。
2. 专业性：传递工作的各种信息，包括情况、经验、问题和对策。
3. 简短性：表达上要内容集中、简明扼要，使读者可以用很短的时间快速掌握信息。

### 行文思考｜输出

根据提供的标题<{{#1727159899881.title#}}>和内容<{{#1727159899881.text#}}>，撰写一个工作简报的提纲。提纲应包含开头、主体、结尾三部分，每一个部分是一个独立的键值对：开头——key为“beginning”、主体——key为“principal”、结尾——key为"ending"。
1. 提纲必须只能以一整个JSON对象的形式输出，对象内不要包含子对象，不要包含数组，不要输出任何markdown格式标记，撰写时必须使用中文。
2. 工作简报是内容比较简略的报道，是机关内部向上级机关汇报工作的一种文字形式，也可把它发送到下属机构或平行机关，用于指导工作、交流经验、互通情报。工作简报具有新闻性、专业性和简短性的特点。
3. 首先，阅读提供的标题和内容，确保理解其背景和核心信息。
4. 根据标题，撰写简报的开头部分，简要介绍背景和目的。
5. 根据内容，撰写简报的主体部分，详细列出工作进展、经验、问题和对策。
6. 撰写简报的结尾部分，总结主要内容，提出下一步工作计划或建议。
7. 确保提纲内容集中、简明扼要，符合工作简报的特点。
8. 输出的提纲不应包含任何XML标签。

---

### 开头｜输入

根据提供的信息，撰写工作简报的开头。简报开头要用概括性的语言介绍时间的主要是适合基本内容。文章的开头和结尾不要撰写任何称谓、总结性、致谢性的内容。撰写完成后直接输出，立刻停止，不要总结你自身的工作。

### 开头｜输出

根据提供的信息<{{#1727160193793.beginning_result#}}>，撰写工作简报的开头。简报开头要用概括性的语言介绍时间的主要内容和基本背景。文章的开头和结尾不要撰写任何称谓、总结性、致谢性的内容。撰写完成后直接输出，立刻停止，不要总结你自身的工作。
1. 仔细阅读提供的信息，确保理解所有关键点。
2. 用概括性的语言撰写简报的开头，介绍时间的主要内容和基本背景。
3. 确保开头部分简洁明了，能够快速传达核心信息。
4. 避免使用任何称谓、总结性、致谢性的内容。
5. 完成撰写后直接输出，不要添加任何额外的内容或总结。

#### 开头代码

```
def main(beginning_input: str) -> dict:
    import json
    try:
        # Parse the input JSON string
        data = json.loads(beginning_input)
        
        # Extract the "beginning" content
        beginning_result = data.get("beginning", "")
        
        # Return the extracted beginning content
        return {"beginning_result": beginning_result}
    
    except json.JSONDecodeError as e:
        # Return error message if JSON is invalid
        return {"error": str(e)}
```

---

### 主体｜输入

根据提供的信息，撰写工作简报的主体。工作简报的主体紧扣开头进行阐述，要按照一定逻辑编写。文章的开头和结尾不要撰写任何称谓、总结性、致谢性的内容。撰写完成后直接输出，立刻停止，不要总结你自身的工作。

### 主体｜输出

根据提供的信息<{{#1727160197295.principal_result#}}>，撰写工作简报的主体。工作简报的主体应紧扣开头<{{#1727525977557.result#}}>进行阐述，按照一定的逻辑编写。文章的开头和结尾不要撰写任何称谓、总结性、致谢性的内容。撰写完成后直接输出，立刻停止，不要总结你自身的工作。
1. 首先，仔细阅读提供的信息，确保理解所有关键点。
2. 根据提供的信息，撰写工作简报的主体部分。确保内容紧扣开头进行阐述。
3. 按照逻辑顺序组织内容，确保条理清晰。
4. 避免在文章的开头和结尾撰写任何称谓、总结性、致谢性的内容。
5. 完成撰写后，直接输出内容，不要进行额外的总结或解释。
6. 输出的内容不应包含任何XML标签。

#### 主体代码

```
def main(principal_input: str) -> dict:
    import json
    try:
        # Parse the input JSON string
        data = json.loads(principal_input)
        
        # Extract the "principal" content
        principal_result = data.get("principal", "")
        
        # Return the extracted principal content
        return {"principal_result": principal_result}
    
    except json.JSONDecodeError as e:
        # Return error message if JSON is invalid
        return {"error": str(e)}
```

#### 首尾呼应代码

```
def main(arg1: str) -> dict:
    return {
        "result": arg1,
    }

```

---

### 结尾｜输入

根据提供的信息，撰写工作简报的结尾。工作简报的结尾要指出时间的意义或者未来发展趋势。文章的开头和结尾不要撰写任何称谓、总结性、致谢性的内容。撰写完成后直接输出，立刻停止，不要总结你自身的工作。

### 结尾｜输出

根据提供的信息<{{#1727160200591.ending_result#}}>，撰写工作简报的结尾。工作简报的结尾要指出时间的意义或者未来发展趋势。文章的开头和结尾不要撰写任何称谓、总结性、致谢性的内容。撰写完成后直接输出，立刻停止，不要总结你自身的工作。
1. 仔细阅读提供的信息，理解其核心内容和背景。
2. 根据信息，撰写一个简短的结尾段落，长度约为5-8句话。
3. 结尾段落应强调时间的意义或未来的发展趋势。
4. 确保结尾段落与提供的信息紧密相关，逻辑清晰。
5. 不要在结尾段落中使用任何称谓、总结性或致谢性的内容。
6. 撰写完成后直接输出，不要添加任何额外的内容或总结。

#### 结尾代码

```
def main(ending_input: str) -> dict:
    import json
    try:
        # Parse the input JSON string
        data = json.loads(ending_input)
        
        # Extract the "ending" content
        ending_result = data.get("ending", "")
        
        # Return the extracted ending content
        return {"ending_result": ending_result}
    
    except json.JSONDecodeError as e:
        # Return error message if JSON is invalid
        return {"error": str(e)}
```

---

## 专题简报

### 行文思考｜输入

根据提供的标题<>和内容<>，撰写一个专题简报的提纲。
提纲应包含开头，主体，结尾三部分。
简报是内容比较简略的报道，是机关内部向上级机关汇报工作的一种文字形式，也可把它发送到下属机构或平行机关，泳衣指导工作、交流经验、互通情报。
而专题简报是针对某项工作、任务、活动而写的，目标单一，有较强的针对性。专题简报的编写是伴随这某项工作、任务、活动的开展而进行的，工作、任务、活动宣告结束，简报的编写也就停止了，因而，它比工作简报更看重时效性。

### 行文思考｜输出

根据提供的标题<{{#1727159899881.title#}}>和内容<{{#1727159899881.text#}}>，撰写一个专题简报的提纲。提纲应包含开头、主体、结尾三部分，每一个部分是一个独立的键值对：开头——key为“beginning”、主体——key为“principal”、结尾——key为"ending"。
1. 提纲必须只能以一整个JSON对象的形式输出，对象内不要包含子对象，不要包含数组，不要输出任何markdown格式标记，撰写时必须使用中文。
2. 专题简报是针对某项工作、任务、活动而写的，目标单一，有较强的针对性。专题简报的编写是伴随这某项工作、任务、活动的开展而进行的，工作、任务、活动宣告结束，简报的编写也就停止了，因而，它比工作简报更看重时效性。
3. 首先，阅读提供的标题和内容，确保理解任务的背景和目的。
4. 根据标题，撰写简报的开头部分，简要介绍任务的背景、目的和重要性。
5. 根据内容，撰写简报的主体部分，详细列出任务的主要内容、进展、成果和存在的问题。
6. 撰写简报的结尾部分，总结任务的总体情况，提出下一步的工作计划或建议。
7. 确保提纲结构清晰，逻辑严谨，语言简洁明了。
8. 输出的提纲不应包含任何XML标签。

---

## 会议简报

### 行文思考｜输入

根据提供的标题<>和内容<>，撰写一个会议简报的提纲。提纲应包含开头，主体，结尾三部分。提纲必须只能以一整个JSON对象的形式输出，对象内不要包含子对象，不要包含数组，不要输出任何markdown格式标记，撰写时必须使用中文。
简报是内容比较简略的报道，是机关内部向上级机关汇报工作的一种文字形式，也可把它发送到下属机构或平行机关，用于指导工作、交流经验、互通情报。
会议简报是在会议期间为了反映会议情况而写的简报。它可以是一次性的，也可以是连续性的。其内容主要包括：主要的报告、讲话、会议决议、讨论发言、会议动态及其它重要状况。

### 行文思考｜输出

根据提供的标题<{{#1727159899881.title#}}>和内容<{{#1727159899881.text#}}>，撰写一个会议简报的提纲。提纲应包含开头，主体，结尾三部分，每一个部分是一个独立的键值对：开头——key为“beginning”、主体——key为“principal”、结尾——key为"ending"。
1. 提纲必须只能以一整个JSON对象的形式输出，对象内不要包含子对象，不要包含数组，不要输出任何markdown格式标记，撰写时必须使用中文。
2. 首先，阅读提供的标题和内容，确保理解会议的主要议题和讨论点。
3. 根据标题和内容，撰写会议简报的开头部分，简要介绍会议的背景和目的。
4. 撰写会议简报的主体部分，详细列出会议的主要报告、讲话、会议决议、讨论发言、会议动态及其他重要状况。
5. 撰写会议简报的结尾部分，总结会议的主要成果和后续行动计划。
6. 确保输出中不包含任何XML标签。

---

## 动态简报

### 行文思考｜输入

根据提供的标题<>和内容<>，撰写一个动态简报的提纲。提纲应包含开头，主体，结尾三部分。提纲必须只能以一整个JSON对象的形式输出，对象内不要包含子对象，不要包含数组，不要输出任何markdown格式标记，撰写时必须使用中文。
简报是内容比较简略的报道，是机关内部向上级机关汇报工作的一种文字形式，也可把它发送到下属机构或平行机关，用于指导工作、交流经验、互通情报。
动态简报是反映本单位、本系统的思想、政治、经济、文化等方面情况、信息而编写的综合性简报。动态简报着重反映与本单位工作有关的正反两方面的新情况、新动向、新问题，为领导和有关部门研究工作提供鲜活的第一手资料，向群众报告工作、学习、生产、思想的最新动态。

### 行文思考｜输出

根据提供的标题<{{#1727159899881.title#}}>和内容<{{#1727159899881.text#}}>，撰写一个动态简报的提纲。提纲应包含开头，主体，结尾三部分，每一个部分是一个独立的键值对：开头——key为“beginning”、主体——key为“principal”、结尾——key为"ending"。
1. 提纲必须只能以一整个JSON对象的形式输出，对象内不要包含子对象，不要包含数组，不要输出任何markdown格式标记，撰写时必须使用中文。
2. 首先，阅读提供的标题和内容，确保理解其核心信息和背景。
3. 根据标题和内容，撰写简报的开头部分，简要介绍背景和目的。
4. 撰写简报的主体部分，详细描述内容中的关键信息，包括新情况、新动向、新问题等。
5. 撰写简报的结尾部分，总结简报内容，提出建议或展望。
6. 确保输出的JSON对象中不包含任何XML标签。